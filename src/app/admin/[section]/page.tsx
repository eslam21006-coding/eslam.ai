import { notFound } from "next/navigation";

/** Unimplemented admin destinations stay unavailable until they are real product surfaces. */
export default function AdminSectionPage() {
  notFound();
}
