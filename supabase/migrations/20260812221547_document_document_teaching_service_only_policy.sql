comment on table public.document_teaching_uploads is
  'Admin-only document teaching upload lifecycle. Access is service-role only through server actions; uploaded rows are immutable provenance and never create Brain content directly.';
